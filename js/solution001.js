//8KYU L1:Set Alarm
// are you employeed? Are you on vacation? Return answer/ do you need an alarm

function setAlarm(employed, vacation) {
  return employed == true && vacation == false ? true : false
}
